// Off-device JVM/Spock harness root project (task 1.3, R18.1).
//
// Layout reconciliation (docs/deploy.md §2): the design's intended `test/`
// Gradle project is realized by EXTENDING the repo's existing root build
// (build.gradle) rather than nesting a second project. The single root build
// compiles src/ (Hubitat app + drivers) and libraries/ (pure DAB v2 modules)
// on the JVM and runs the Spock suite under tests/.
rootProject.name = 'hubitat-flair-vents'
