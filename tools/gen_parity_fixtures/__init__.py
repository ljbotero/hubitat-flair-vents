"""gen_parity_fixtures — regenerate parity ``*.expected.json`` from the Reference.

See ``__main__.py`` for the full module docstring and CLI. The package is
importable so the design's ``python -m tools.gen_parity_fixtures`` form works
when the repo root is on ``sys.path``; the canonical invocation is
``python tools/gen_parity_fixtures`` from the hubitat-flair-vents working copy.
"""
